# Local backend environment - Development
# Security/session settings are hardcoded in settings.py

DJANGO_SECRET_KEY=dev-secret-key-for-local-only
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

DATABASE_URL=sqlite:///db.sqlite3

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://localhost:3000

# API Rate Limiting (generous for development)
REST_FRAMEWORK_ANON_THROTTLE_RATE=1000/hour
REST_FRAMEWORK_USER_THROTTLE_RATE=10000/hour

# Brute Force Protection (django-axes) - generous for dev
AXES_FAILURE_LIMIT=100
AXES_COOLOFF_DURATION=600

# JWT Token Lifetimes
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=5
JWT_REFRESH_TOKEN_LIFETIME_HOURS=24

# Contact Form Security (per IP per 24 hours) - generous for development
CONTACT_FORM_RATE_LIMIT=2
CONTACT_FORM_TIME_WINDOW=86400



# Production backend environment - Minimal config
# Security/session settings are hardcoded in settings.py

DJANGO_SECRET_KEY=key
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=
DJANGO_SETTINGS_MODULE=

# Database
DATABASE_URL=

# CORS and CSRF
CORS_ALLOWED_ORIGINS=
CSRF_TRUSTED_ORIGINS=

# API Rate Limiting
REST_FRAMEWORK_ANON_THROTTLE_RATE=100/hour
REST_FRAMEWORK_USER_THROTTLE_RATE=1000/hour

# Brute Force Protection (django-axes)
AXES_FAILURE_LIMIT=20
AXES_COOLOFF_DURATION=3600

# JWT Token Lifetimes
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=5
JWT_REFRESH_TOKEN_LIFETIME_HOURS=24

# Contact Form Security (per IP per 24 hours)
CONTACT_FORM_RATE_LIMIT=3
CONTACT_FORM_TIME_WINDOW=86400

